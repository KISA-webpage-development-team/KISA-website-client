기능: 이전 포차들을 가져온다.

1. FE는 무엇을 해야하는가?
- UI: list of pocha cards (card component는 이미 있음! - PochaSummary.tsx)
- API Call - BE에서 이전 포차 정보를 가져온다 (returns list of pocha info): 
    GET /api/v2/pocha/previous/

2. BE는 무엇을 해야하는가?
- Define API endpoint
- Implement API logic
step 1. pocha table에서 어떤 pocha들? pocha.endDate < current time
-> list of pocha

step 2. return list pocha info